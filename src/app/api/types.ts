export type Reward = {
  name: string;
  cost: number;
};

export type Profile = {
  id: string;
  name: string;
  points: number;
  group_size: string;
  goal: string;
  distance: string;
  age: number;
  email: string;
  photoURL?: string;
};
